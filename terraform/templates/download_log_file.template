{
  "clientIp": "$input.params('X-Forwarded-For')",
  "sourceIP": "$context.identity.sourceIp",
  "requestId": "$context.requestId",
  "resourcePath": "$context.resourcePath",
  "parameters": {
    "query": {
      "file_path": "$input.params().querystring.get('file_path')"
    }
  }
}
